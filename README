lzip
------------------------------------------
Lzip is a lossless data compressor based on the LZMA algorithm, with 
very safe integrity checking and a user interface similar to the one of 
gzip or bzip2. Lzip decompresses almost as fast as gzip and compresses 
better than bzip2, which makes it well suited for software distribution 
and data archiving.

Runtime requirements:
  cygwin-1.7.0-62
  libgcc1-4.3.4-1
  libstdc++6-4.3.4-1

Build requirements:
(besides corresponding -devel packages)
  binutils-2.19.51-1
  cygport-0.9.10-1
  gcc4-core-4.3.4-1
  gcc4-g++-4.3.4-1
  make-3.81-2

Canonical website:
  http://www.nongnu.org/lzip/lzip.html

Canonical download:
  http://download.savannah.gnu.org/releases/lzip/

-------------------------------------------

Build instructions:
  unpack lzip-1.8-X-src.tar.bz2
    if you use setup to install this src package, it will be
	 unpacked under /usr/src automatically
  cd /usr/src
  cygport ./lzip-1.8-X.cygport all

This will create:
  /usr/src/lzip-1.8-X-src.tar.bz2
  /usr/src/lzip-1.8-X.tar.bz2

For a list of files included in the binary package, run from the 
command-line: 'cygcheck -l lzip'.

-------------------------------------------

Port Notes:

----- version 1.8-1 -----
Initial release for Cygwin 1.7.


Cygwin port maintained by:
http://sourceware.org/cygwinports/
